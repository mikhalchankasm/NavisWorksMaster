# Primary Developer (retired)

This role document no longer carries instructions. The branch discipline it used
to describe — one task branch per task from the current `origin/main`, no direct
development or push to `main` — is now in section 1 of
[AGENTS.md](../../AGENTS.md), which is the only document you must read before a
task.

There is no Primary/Secondary split any more: both workstations follow the same
rules, so there is nothing to look up per role and no role to ask about.

Kept as a file so existing links do not break.
