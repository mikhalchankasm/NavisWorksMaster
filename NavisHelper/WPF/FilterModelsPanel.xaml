<Window x:Class="NavisHelper.WPF.FilterModelsPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Фильтр по списку" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Путь к файлу:" Grid.Row="0" Grid.Column="0" Margin="0,0,10,5"/>
        <Grid Grid.Row="0" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtFilePath" Grid.Column="0" Margin="0,0,5,0"/>
            <Button x:Name="btnBrowse" Content="Обзор" Grid.Column="1" Click="btnBrowse_Click"/>
        </Grid>

        <Label Content="Тип поиска:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,5"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
            <RadioButton x:Name="rbExact" Content="Точное совпадение" IsChecked="True" Margin="0,0,10,0"/>
            <RadioButton x:Name="rbPartial" Content="Частичное совпадение"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button x:Name="btnFilter" Content="Применить фильтр" Click="btnFilter_Click" Margin="0,0,10,0"/>
            <Button x:Name="btnReset" Content="Сбросить" Click="btnReset_Click"/>
        </StackPanel>

        <Label Content="Статус:" Grid.Row="3" Grid.Column="0" Margin="0,0,10,0"/>
        <TextBlock x:Name="txtStatus" Grid.Row="3" Grid.Column="1" TextWrapping="Wrap"/>
    </Grid>
</Window> 